// 1. 알파벳 대소문자 판별 함수
// const word1 = window.prompt("문자(알파벳)을 하나 입력해주세요:");
// function uppercase_check(x) {
//   const is_uppercase = x == x.toUpperCase();
//   is_uppercase ? window.console.log(`${x}는 대문자입니다.`) : window.console.log(`${x}는 소문자입니다.`);
//   return 0;
// }
// uppercase_check(word1);


// 2. 문자 포함 여부 판별 함수
// const string2 = window.prompt("문자열을 하나 입력해주세요:"); 
// const word2 = window.prompt("문자(알파벳)을 하나 입력해주세요:");
// function include_word2_check(x, y) {
//   const is_have_word2 = x.includes(y);
//   is_have_word2 ? window.console.log(`${y}가 포함되어 있습니다.`) : window.console.log(`${x}에는 ${y}가 포함되어 있지 않습니다.`);
//   return 0;
// }
// include_word2_check(string2, word2);

// 3. 이메일 형식 판별 함수
// const string3 = window.prompt("이메일을 입력해주세요:"); 
// function email_check(x) {
//   is_have_special_icon = x.includes("@");
//   is_have_special_icon ? window.console.log(`${x}(은)는 올바른 형식입니다.`) : window.console.log(`${x}(은)는 올바르지 않은 이메일 형식입니다.`);
//   return 0;
// }
// email_check(string3);
